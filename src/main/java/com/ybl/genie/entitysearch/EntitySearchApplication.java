package com.ybl.genie.entitysearch;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication(scanBasePackages="com.ybl.genie.entitysearch")
public class EntitySearchApplication {
    public static void main(String[] args) {
        SpringApplication.run(EntitySearchApplication.class, args);
    }
}
